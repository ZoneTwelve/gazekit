mediapipe>=1.0.0
opencv-python>=4.10
numpy>=1.26
scikit-learn>=1.4
torch>=2.4
torchvision>=0.19
# Florence-2 remote code needs pre-5.x transformers (generation params
# were moved out of PretrainedConfig later)
transformers==4.49.0
timm
einops
pandas>=2.0
# ambient overlay window (macOS)
pyobjc-framework-Cocoa; sys_platform == "darwin"
# `gazekit publish` (Hub sync); small, no model downloads
huggingface_hub>=0.26
